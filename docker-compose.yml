services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jpecom-service
    restart: unless-stopped
    ports:
      - "3004:3004"
    environment:
      - NODE_ENV=production

volumes: {}
